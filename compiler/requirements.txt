# Required for coordinate projection (lat/lon -> projected meters).
pyproj>=3.6

# Optional: only needed for `geoworld_compiler preview` (PNG debug renders).
# pillow>=10.0

# Phase 3+: real GIS ingestion.
# rasterio>=1.3
# geopandas>=1.0
# shapely>=2.0
